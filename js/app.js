import { registerWithEmail, loginWithEmail } from './auth.js';
import { isUsernameTaken, saveUserData, getUserData } from './db.js';

document.addEventListener('DOMContentLoaded', () => {
    // سيتم ربط الواجهات والتحكم بالتبويبات الأربعة هنا فور اكتمال رفع ملفات المصادقة
    console.log("Manbr Telegram Initialized successfully.");
});
